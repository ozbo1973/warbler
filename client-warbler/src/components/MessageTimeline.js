import React from "react";
import MessagList from "../containers/MessagList";

const MessageTimeline = props => {
  return (
    <div className="row">
      <MessageList />
    </div>
  );
};

export default MessageTimeline;
